<UserControl x:Class="MedievalWarfare.TestClient.View.UnitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:l="clr-namespace:MedievalWarfare.TestClient.Utils">
    <UserControl.Resources>
        <l:StringToImageConverter x:Key="StringToImage"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Image HorizontalAlignment="Center" VerticalAlignment="Center" MaxHeight="30" MaxWidth="30" Source="{Binding Path=Type, Converter={StaticResource StringToImage}}" />
        <!--<LineBreak/>-->
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="1" Text="{Binding Path=Owner.Name}" />
    </Grid>
</UserControl>
